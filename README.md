This is a repo for Grievances Portal for MSIT
It is built using `Next.js`, `TypeScript`, `MongoDB`, `Next-auth` and `TailwindCSS`

feature1-
- remove _id from any fields that appear on client side.
    - create a function that performs the cleaning formatting of data fetched from db before passing it to client side.
- allow admins to update status of a complaint.
- allow users to update status of a complaint.